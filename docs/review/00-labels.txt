REVIEW BATCH — 2026-08-03
Two visual changes shipped this session. Each is a BEFORE/AFTER pair.
All crops: screen Y 560..844 (queue rows, bench, booster row), deviceScaleFactor 3.

===============================================================================
PAIR A — BOTTOM-CHROME RECLAIM  (shipped, master 4c01e92)
===============================================================================
01-reclaim-BEFORE-L5.png    L5 before
02-reclaim-AFTER-L5.png     L5 after
03-reclaim-BEFORE-L8.png    L8 before
04-reclaim-AFTER-L8.png     L8 after

WHAT CHANGED: reclaimed 32px of bottom chrome (24px dead strip below the booster
row + 8px of bench gap/padding) and spent all of it on bomb size.
    queue scale  0.4923 -> 0.6553
    ball radius  11.33px -> 14.21px   (+25%)

WHAT TO LOOK FOR:
  - The bombs are visibly BIGGER in the AFTER shots. This is the "bombs look
    small" complaint, raised twice.
  - The booster row (COLOR / FREEZE / BOMB) sits LOWER and now runs flush to the
    bottom edge. The empty strip beneath it in BEFORE is gone.
  - The bench strip sits directly under the queue with no separating gap.
  - Third row of bombs clears the bench cleanly in AFTER.

>>> STILL NEEDS YOUR DEVICE: the booster cards now end 2px above the screen
    bottom (was 26px), and there is NO safe-area handling in the source. On a
    phone with a gesture bar / home indicator, the lower part of the cards —
    including BOMB — may fall inside the system gesture zone. Please check
    whether the boosters feel reliable to tap. Revert is two constants
    (BoosterBar.BAR_Y + its projection mirror) if they don't.

===============================================================================
PAIR B — BALL/SOCKET ALIGNMENT  (shipped, master e7b4720)
===============================================================================
05-alignment-BEFORE-L13toL5.png   the reported bug
06-alignment-AFTER-L13toL5.png    fixed

HOW THESE WERE CAPTURED — this matters: load L13 (4-lane, band 540) FIRST, then
L5 (3-lane, band 600). A fresh L5 load does NOT reproduce the bug, because the
stale value latches from the FIRST level of the session. A rest screenshot of L5
alone would have shown a perfectly clean queue.

WHAT TO LOOK FOR IN 05 (BEFORE) — this is your "bombs stuck out of their slots":
  - Empty socket RINGS are visible sitting at their correct positions, with the
    BALLS floating between them rather than inside them.
  - The misalignment GROWS down the column: row 0 nearly right, row 2 badly off.
  - The bottom row's balls are cut off by the bench strip.

WHAT TO LOOK FOR IN 06 (AFTER):
  - Every ball centred in its socket, all three rows, both compared side by side.
  - Bottom row clears the bench strip with visible margin.

CAUSE: Shooter3D.update() latched the slot Z once and then pinned position to it
every frame. setLaneCount() assigned the new band's slot Z and the next update
tick silently undid it — so the balls used the pitch of the session's FIRST level
forever, while the Pixi sockets recomputed live. Fix: read slotZ() live, no cache.

>>> STILL NEEDS YOUR DEVICE: please confirm alignment holds when you actually
    cross a band boundary in play (e.g. finish L8 -> L9, or L13 -> L5 via level
    select), not just on a fresh level load.
