import { describe, it } from 'vitest'

describe('Fairness Rules', () => {
  it.todo('FR-1: at least 1 shooter color-matches at least 1 front car')
  it.todo('FR-2: at most 3 of 4 front cars share the same color')
  it.todo('FR-3: average shooter damage >= 50% of average front car HP')
  it.todo('FR-4: no car HP exceeds 2.5x the highest available shooter damage')
  it.todo('FR-5: at least 2 distinct colors in the top shooter row')
})
